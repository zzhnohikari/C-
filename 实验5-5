#include<stdio.h>
void cat_rew(char s[])
{
	int i,j,k;
	j=i=strlen(s);
	for(i-=1;i>=0;i--)
	{
		s[j++]=s[i];	
	}
	s[j]='\0';
	puts(s);
}
int main ()
{
	char s[128];
	gets(s);
	cat_rew(s);
	return 0;
}
